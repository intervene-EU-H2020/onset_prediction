create_coxph_res_dt <- function(coxph_res,
                                endpt,
                                score_type,
                                N_controls,
                                N_cases) {

    coxph_res <- extract_coxph_res(coxph_res)

}